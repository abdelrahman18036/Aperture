{
  "name": "@repo/realtime-events",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Zod schemas for socket events, shared by the Node gateway and the browser.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "check-types": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "typescript": "5.9.3",
    "vitest": "4.1.10"
  }
}
