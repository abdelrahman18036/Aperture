# Hooks

Cross-feature hooks only. A hook used by exactly one feature belongs in that
feature's folder under `src/features/`, not here.

Empty until something is genuinely shared. See `01-ARCHITECTURE.md` §2.
