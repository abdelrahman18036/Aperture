/**
 * Placeholder root route.
 *
 * Phase 1 builds the foundation, not the UI — the design system is Phase 2
 * and the feed is Phase 4. This exists so the dev server has something to
 * serve at `/`, and it is deliberately unstyled: anything decorative here
 * would be a flourish the spec did not ask for.
 */
export default function Home() {
  return (
    <main>
      <h1>Aperture</h1>
      <p>Phase 1 — foundation. The UI starts in Phase 2.</p>
    </main>
  );
}
